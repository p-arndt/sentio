services:
  sentio:
    build:
      context: ..
      dockerfile: Dockerfile
    restart: always
    ports:
      - 8080:8080
    env_file: .env
    depends_on:
      - postgres

  postgres:
    image: postgres:17
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER:
      POSTGRES_PASSWORD:
      POSTGRES_DB:
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
